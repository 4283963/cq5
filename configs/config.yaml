server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: incubator
  sslmode: disable
  timezone: Asia/Shanghai
  max_idle_conns: 10
  max_open_conns: 100

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0
  pool_size: 10
  cache_ttl: 3600

jwt:
  secret: incubator-secret-key-2024
  expire_hours: 24

device:
  status_cache_prefix: "device:status:"
  config_cache_prefix: "device:config:"
